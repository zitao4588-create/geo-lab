# G4 失败与处理记录

1. 首个测试因 operations/rate limiter 模块缺失失败；实现后核心 4/4。
2. artifact scan 测试先因脚本不存在失败；新增扫描脚本后通过，且测试证明只返回规则名、不回显疑似 secret。
3. 成本门最初只有布尔确认，审计发现可能被永久遗忘；收紧为布尔 + 未来截止时间，过期自动 fail-close，并增加 0 请求测试。
4. PageAudit 计时首次补丁把变量放在 preflight 分支，TypeScript 审查发现诊断分支缺少变量；在进入回归前修正到实际 POST 路径。

最终没有跳过旧测试：78/78。
