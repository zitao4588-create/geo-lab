# 失败与处理记录

1. 初始内容契约 0/4：按预期证明定义、FAQ、sitemap 和 PageAudit 基建缺口；完成最小内容与审计修复后 4/4。
2. 首次全量 `npm test`：55 通过、8 失败。8 项均为沙箱禁止监听 `127.0.0.1` 的 `EPERM`，不是断言失败；获准在沙箱外复跑后 63/63。
3. 首次 TypeScript 回归：`input.links` 可能为空。改为对可选字段使用空字符串 fallback 后编译与回归通过。

未发生真实 provider 调用、生产写入、部署、提交或推送。
