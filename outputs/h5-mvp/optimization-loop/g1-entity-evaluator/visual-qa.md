# G1 视觉 QA

工具：Playwright CLI 隔离浏览器会话。

验证状态：

1. 型号冲突 preflight：显示“需要确认”、ES-LV9C/ES-LM55 冲突说明、修正动作以及“未消耗免费名额/未调用模型”。
2. 双指标报告：同时显示“提交来源可信度 100”和“站点基建完整度 0”，摘要、报告卡和公开基建区语义一致。

结果：

- 390×844：两种状态均通过，无横向溢出。
- 1440×1000：两种状态均通过，无横向溢出。
- console error：0。
- console warning：0。
- 截图均来自本地真实 H5；报告采样数据为明确标注的确定性 fixture，不调用真实模型。

证据目录：`screenshots/`。
