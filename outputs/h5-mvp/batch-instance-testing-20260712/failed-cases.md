# 失败与修复记录

## 本轮发现并修复

1. **L02 / P1 本地服务路由错误**：城市为“全国”仍返回 ready。现要求具体城市、门店或服务半径；由 batch-instance-matrix.test.mjs 回归。
2. **R03 / P1 部分失败未降低置信度**：PageAudit 100 时一半 provider 失败仍显示高置信度。现合并采样覆盖率；由 report-credibility.test.mjs 回归。
3. **L04 / P0 用户承诺被列入“已确认”**：未证明的价格、效果和安全主张被当成已确认事实。现只确认业务类型、目标客户和 PageAudit 命中事实，用户描述进入“仍待核验”；由 unsupported-claims 回归和刷新后的双端截图验证。
4. **阶段 B / P0 暂停评分仍泄露内部总分**：真实 S01 主封面显示“暂不评分”，但桌面预览、摘要、Markdown 和 HTML 仍出现 `44/100`。现所有用户表面都按 `scoreStatus` 输出暂停评分语义，evidence package 增加 `displayedScore: null`；25% 覆盖回归和真实双端截图通过。

## 当前失败

阶段 A 20/20 通过。阶段 B 当前有以下未解决项：

1. **P1 / provider 运行态**：百炼 DeepSeek、百炼 Qwen 和腾讯 Hy3 对当前出口 IP 返回 403 allowlist/access denied。五份报告中三家均为 0/10，只有火山方舟 10/10。权限类错误不再重试，也没有修改云端白名单。
2. **P1 / 官方来源 PageAudit 假阴性**：已人工核验的松下 ES-LM55 官方页在 P01/P02 中只得到 14/100、0 个通过；海底捞入口在 L01/L03 中得到 41/100、0 个通过。这会让正确实体识别率变成 N/A，并阻止 P02 的型号冲突判断。保护行为正确，但产品可用性不足。
3. **执行器安全缺口（已修复，未重跑）**：首次阶段 B runner 只把 billing/quota 视为停止信号，导致相同 403 权限错误继续覆盖后续实例。`run-stage-b.mjs` 已增加 access/allowlist 停止规则；为避免重复消耗火山免费额度，没有重跑。

没有发现 P0：5/5 新报告均显示低置信度、覆盖率缺失并暂停总分；失败平台未被模拟答案填充。
